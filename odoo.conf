[options]
; ═══════════════════════════════════════════════════════════════
; ODOO 19 - Configuración Default
; Proyecto: MayordomIA / AlianeD
; NOTA: db_host, db_port, db_user, db_password se configuran
;       via environment variables en docker-compose.yml
; ═══════════════════════════════════════════════════════════════

addons_path = /opt/odoo/addons,/mnt/extra-addons
data_dir = /var/lib/odoo

http_interface = 0.0.0.0
http_port = 8069

admin_passwd = alianed_master_2026
list_db = True

workers = 0
max_cron_threads = 1
limit_memory_hard = 2684354560
limit_memory_soft = 2147483648
limit_request = 8192
limit_time_cpu = 600
limit_time_real = 1200

log_level = info

smtp_server = False
without_demo = False
